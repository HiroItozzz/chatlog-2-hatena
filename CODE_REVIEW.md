# コードレビュー（初期実装）

- 実施日: 2026-09-23
- 対象: `refac/mcp` ブランチ。テストの書き直しと `blog_schema` → `schema` の import 修正の**前**の状態

## 総評

学習の最初期の作品としては上出来。ただし、長く保守するコードとしては作り直しが必要な段階。
責務ごとのパッケージ分割、ABC、Pydantic、非同期での並列投稿といった設計の方向性は筋がよい。一方で、エラー処理と初期化の場所が場当たり的で、バグはそこに集中している。MCP 化の前に解消すべき箇所も同じところにある。

| 観点 | 点 | 理由 |
|---|---|---|
| 設計の方向性 | 7/10 | 分割と抽象化の発想はよい |
| 正しさ | 4/10 | エラー経路にバグが集中 |
| 保守性 | 4/10 | 神関数、import 時の副作用 |
| テスト | 2/10 | 実通信・実ファイル書き込みを行っており、単体テストとして機能していなかった |
| ドキュメント | 8/10 | README と依存問題の調査メモの質が高い |

## 良い点

- **層が分かれている。** `llm/`・`blog/`・読み込み・設定が分離されている。LLM は `ConversationalAi`（ABC）とモデル名からのファクトリで差し替えられるようになっており、MCP 化にも耐えられる土台になっている。
- **LLM 出力を構造化している。** Gemini の出力を `AiOutput` スキーマで JSON に固定してから XML に変換している。
- **XML を ElementTree で組み立てている。** 文字列連結ではないので、エスケープ漏れが起きない。
- **並列投稿が他のサービスに影響しない。** `asyncio.gather(..., return_exceptions=True)` により、1つのサービスが失敗しても他の投稿は止まらない。
- **DEBUG 時は自動で下書き投稿になる。** 本番事故を防ぐ仕組みとして有効。
- **実際の API レスポンスを `sample/` に保存している。** テストのフィクスチャとしてそのまま使える。
- **`DEPENDENCY_COMPATIBILITY.md` がある。** httpx / httpx2 と Authlib の相性問題を、原因まで掘り下げて記録している。

## 問題点

### 1. 実際に起きるバグ

- **`main.py` の結果集計で `hatena_error` が上書きされる。** はてなが失敗したあとに Qiita も失敗すると `False` に戻る。さらに、はてなが失敗すると `hatena_result` が未定義のまま参照されて `NameError` になり、それを外側の `except` が握りつぶしている。
- **はてなのキーが空でも投稿しに行く。** 空文字の `HatenaSecretKeys` でも検証を通るため、`if schema.hatena_secret_keys:` が常に真になる。
- **モデル名が不正なときに正しく止まらない。** `create_ai_client`・`setup.config_setup` はログを出すだけで処理を続けるため、その後 `UnboundLocalError` になる。
- **Qiita の `preset_cats` が `max_length=1`。** プリセットカテゴリを2つ設定すると、Qiita 投稿だけ `ValidationError` になる。
- **`to_spreadsheet` がシート名の設定を無視する。** シートが見つからない場合、設定された名前ではなく固定の `"record"` という名前で作成する。
- **`line_message.py` の認証ヘッダーが `Bearer <token>` になっている。** 山括弧はドキュメント上の「ここに入れる」という記号なので、認証で弾かれている可能性が高い（未確認）。`requests.post` にタイムアウトもない。
- **`json_loader.convert_to_str` の例外ケース。** 最後のメッセージに時刻がなく、他のメッセージにはある場合、`dt_format` が未定義で落ちる。`messages` が空の場合は `IndexError`。

### 2. 設計（MCP 化の障害になる箇所）

- **ライブラリ層で `sys.exit()` している。** `ConversationalAi` のエラーハンドラ、`check_response`、`DeepseekClient` が該当。サーバーから呼ぶとプロセスごと終了してしまう。ライブラリ層は例外を投げ、終了するかどうかは CLI 側だけで判断するべき。
- **import しただけで副作用が起きる。** `main.py` を import すると、設定の読み込み・ロガー設定・失敗時の `sys.exit` がすべて走る。`setup.get_DEBUG(config=get_yaml_config())` は、デフォルト引数が評価される時点で config を読み込む。DEBUG の判定ロジックも2か所に重複している。
- **`main()` が約200行の神関数になっている。** 投稿・通知・為替取得・CSV・Sheets の処理が同じ関数に集中している。
- **BaseModel による結合が強すぎる。** 各ポスターは共通スキーマを `model_dump()` し、alias でフィールドを対応付けて生成されるため、フィールド名への依存が強い。Dev.to の `published` は alias `is_draft` で受け取ってから `model_post_init` で反転させており、読み手を混乱させる。再初期化されると二重に反転する危険もある。
- **失敗時の返り値がサービスごとに違う。** Qiita は `dict` を返し、Dev.to は `raise_for_status` で例外を投げ、はてなは失敗しても XML を解析しに行く。`AbstractBlogPoster.blog_post` の抽象メソッドのシグネチャも、実装と一致していない。
- **`setup.config_validation` の分岐が脆い。** dict のインデックス番号で分岐しており、出力されるメッセージも実際の分岐内容と一致していない。

### 3. 細部

- **ログレベルを表示するかどうかの切り替えに使っている。** 通常のメッセージまで `logger.warning` で出しており、`print` も混在している。
- **DeepSeek のエラー判定が文字列一致になっている。** `"500" in str(e)` のような判定をしているが、openai SDK にはエラー種別ごとの例外クラスがある。また `self.prompt` を書き換えている。
- **`LlmFee` の料金表が if 分岐にハードコードされている。** 未登録のモデルは `self.model` を書き換えたうえで、2.5-pro の料金で計算される。
- **依存ライブラリが重複している。** requests と httpx、requests-oauthlib と authlib が両方入っている。為替レート1件の取得のために yfinance（pandas を含む）に依存している。
- **テストが単体テストになっていなかった。** 実際の API 通信と `outputs/` への書き込みを行っていた。パスも Windows 形式で書かれていた。

## MCP 化の前にやる順番（提案）

1. ライブラリ層の `sys.exit` をなくし、独自の例外を投げる形にする
2. import 時の初期化をやめ、`setup` は呼ばれたときに設定を返すだけの関数にする
3. ポスターの返り値を「成功なら結果、失敗なら例外」に統一する（はてな・Qiita・Dev.to）
4. 読み込み層をクラス化してインターフェースを挟み、はてなのキーを必須でなくする（`TODO_MCP.md` 参照）
5. `main()` を「処理の流れ（ライブラリ）」と「CLI の出力」に分ける
